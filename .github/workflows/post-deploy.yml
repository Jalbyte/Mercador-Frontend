name: Post Deploy Phase

on:
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Notify success
        run: echo "[OK] Despliegue completado. Iniciando verificación o pruebas..."
